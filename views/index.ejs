<!DOCTYPE html>
<html>
  <head>
    <title>Toy Auth</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    
    <% if (user) { %>
      <h1>Herro, <%= user.name %>!</h1>
        <% if (user.avatar) { %>
          <img src="<%= user.avatar %>">
        <% } else { %>
          <h1>no images</h1>
        <% } %>
      <a href="/logout">Log Out</a>
    <% } else { %>
      <h1>Herro, there!</h1>
      <a href="/auth/google">Log In</a>
    <% } %>
    <hr>
    
  </body>
</html>
